const { VITE_BANDS_URL: BANDS_URL } = import.meta.env;
const { VITE_GENRES_URL: GENRES_URL } = import.meta.env;
const { VITE_ALBUMS_URL: ALBUMS_URL } = import.meta.env;

export { BANDS_URL, GENRES_URL, ALBUMS_URL };
